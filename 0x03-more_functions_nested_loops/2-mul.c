#include "holberton.h"
/**
 * mul - multiplies integers a and b
 * @a: first integer to multiply
 * @b: second integer to multiply
 * Description: multiplies two integers, a and b returns result
 * section header: Section description)*
 * Return: result of two numbers
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
